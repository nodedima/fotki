express построен на connect, 
middleware -- app.use
такой формат коллбэка
(req, res, next)
если next() вызван как next(Error)
то он передаёт обработчику ошибок

app.use(function(err, req, res, next))

также req и res в express
расширены expressom и у них есть новые методы

